# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 | library | shadcn/ui installation path | shadcn canary (Tailwind v4 path) | Project is on Tailwind v4; canary supports CSS-based config, no tailwind.config.ts needed, aligns with existing globals.css approach | Yes — if shadcn v4 goes stable | agent |
| D002 | M001 | arch | Network target | Devnet | User confirmed devnet for all development; mainnet explicitly out of scope until ready | Yes — single env var change | agent |
| D003 | M001 | data | Payment token | USDC (Token-2022 devnet mint) | User confirmed USDC; other Token-2022 mints out of scope for UI | Yes — UI restriction only, contract is generic | agent |
| D004 | M001 | library | Oracle price feed source | Pyth | User confirmed Pyth; standard on Solana devnet, well-documented SDKs | Yes — if Switchboard preferred later | agent |
| D005 | M001 | arch | Design system coexistence | Keep existing GherkinPay CSS tokens, add shadcn tokens alongside | Existing --bg/--green/--surface tokens are used throughout all pages; shadcn needs --background/--primary/--card; both live in globals.css, shadcn components themed to match dark green palette | Yes — could consolidate later | agent |
| D006 | M001 | arch | Wallet adapter strategy | @solana/wallet-adapter-react with ConnectionProvider + WalletProvider in a client boundary | Next.js App Router requires wallet providers in a client component; Wallet Standard auto-detects Phantom and Solflare without explicit adapters | No | agent |
| D007 | M001/S01/T01 | library | How to initialize shadcn/ui in GherkinPay frontend | Manual components.json creation instead of interactive `shadcn init` | Interactive `shadcn init` overwrites globals.css and prompts for input, which is incompatible with automated execution. Manual creation gives full control over alias paths (~/  not @/) and preserves the existing CSS design system. | Yes | agent |
| D008 | M001/S03 | arch | How to handle Anchor 0.32 Program generic type inference in query hooks | Cast to Program<GherkinPay> at query hook level | Anchor 0.32 Program constructor takes `idl: any`, so TypeScript always infers Program<Idl> regardless of IDL object cast. Accessing typed accounts like program.account.paymentAgreement requires the specific generic. Casting at the hook keeps anchor.ts unchanged and is a single-point fix per query file. | Yes | agent |
| D009 | M004/S01 | architecture | How to add Travel Rule metadata to PaymentAgreement struct | Added `metadata_uri: String` with `#[max_len(200)]` as a URI pointer rather than storing PII directly on-chain | Travel Rule compliance requires identity metadata but storing PII on-chain is a privacy risk. A URI pointer to externally-hosted metadata (IPFS, Arweave, HTTPS) keeps the on-chain footprint minimal while allowing compliance verification. 200 chars is sufficient for IPFS CIDs, Arweave tx IDs, and HTTPS URLs. | Yes | agent |
| D010 | M004/S03 | architecture | How to parse Pyth PriceUpdateV2 in the crank bot | Parse at same byte offsets as on-chain program (price @ 73, publish_time @ 93) using bigint for i64/u64 values | Consistency with on-chain program ensures bot evaluates the same price data. Bigint avoids BN precision loss on large Pyth price values that exceed JS Number safe range. | Yes | agent |
| D011 | M006 | scope | Which FX pairs to include as oracle presets | EUR/USD, GBP/USD, JPY/USD — the three major FX pairs | Enough to demonstrate FX settlement capability for Track 3 judging without cluttering the UI. These are the highest-volume institutional FX pairs. | Yes — can add more pairs later | collaborative |
| D012 | M006/S01 | architecture | How to handle Pyth pull-model FX feeds in the oracle crank flow | Unified post+crank for all oracle conditions — HermesClient fetch → PythSolanaReceiver.newTransactionBuilder → addPostPriceUpdates → addPriceConsumerInstructions → buildVersionedTransactions → provider.sendAll | FX feeds aren't push-sponsored on Solana so the price data must be posted before cranking. Rather than maintaining two code paths (push vs pull), all oracle cranks go through post+crank since re-posting an already-pushed price is a no-op. This simplifies the UI and eliminates the need to detect feed type at runtime. | Yes — could split paths if post overhead matters for push feeds | agent |
| D013 | M006/S01 | library | How to handle jito-ts transitive dependency breaking Next.js webpack build | Dynamic import for HermesClient in useOraclePrice; webpack alias `jito-ts: false` in next.config.js | @pythnetwork/pyth-solana-receiver → @pythnetwork/solana-utils → jito-ts → old @solana/web3.js with incompatible rpc-websockets. We never use Jito, so stubbing it is safe. Dynamic import in the price display path avoids pulling the dependency chain into SSR. | Yes — pin rpc-websockets if Jito support is needed later | agent |
