---
id: T01
parent: S03
milestone: M005
provides:
  - gherkin_pay program deployed to devnet with metadataUri account layout
  - synced frontend TypeScript types with 4 metadataUri fields
  - clean frontend production build
key_files:
  - app/web/src/types/gherkin_pay.ts
  - target/deploy/gherkin_pay-keypair.json
key_decisions:
  - Copied program keypair from main repo to worktree since S01 build generated a new keypair; worktree keypairs don't persist across git worktree boundaries
patterns_established:
  - When deploying from a worktree, always verify target/deploy/*-keypair.json matches the expected program ID before anchor deploy
observability_surfaces:
  - "solana program show 2wL3PPjoG4UmVrNYZyXvxfTfV738AVCG8LHJPUEtxEeV --url devnet — deploy slot, data length, authority"
  - "grep -c metadataUri app/web/src/types/gherkin_pay.ts — type sync health (expect 4)"
duration: 8m
verification_result: passed
completed_at: 2026-03-23
blocker_discovered: false
---

# T01: Deploy to devnet and sync frontend types

**Deployed gherkin_pay to devnet at 2wL3PPjoG4UmVrNYZyXvxfTfV738AVCG8LHJPUEtxEeV (slot 450530953), synced frontend types with 4 metadataUri fields, and confirmed frontend builds clean.**

## What Happened

The worktree's `target/deploy/gherkin_pay-keypair.json` was generated by S01's build and mapped to a different program address (`FcvdqJ6CkXhRujXUptNk1DwDcQepRsL2iLCCXvmrg2GY`). The first deploy attempt went to that address. I copied the correct keypair from the main repo (`2wL3PPjoG4UmVrNYZyXvxfTfV738AVCG8LHJPUEtxEeV`) and redeployed successfully.

After deployment, copied `target/types/gherkin_pay.ts` over the stale `app/web/src/types/gherkin_pay.ts`. The stale file had 0 metadataUri occurrences and used the name `"triggerPay"`; the replacement has 4 metadataUri fields and name `"gherkinPay"`.

Ran `bun install` (1423 packages) followed by `bun run build` — Next.js compiled all 10 routes successfully with the synced types.

## Verification

1. `solana program show` confirms program deployed at correct address with data length 422944 bytes
2. `diff target/types/gherkin_pay.ts app/web/src/types/gherkin_pay.ts` returns empty (exit 0)
3. `grep -c metadataUri app/web/src/types/gherkin_pay.ts` returns 4
4. `cd app/web && bun run build` exits 0, all 10 routes compiled

## Verification Evidence

| # | Command | Exit Code | Verdict | Duration |
|---|---------|-----------|---------|----------|
| 1 | `solana program show 2wL3PPjoG4UmVrNYZyXvxfTfV738AVCG8LHJPUEtxEeV --url devnet` | 0 | ✅ pass | 2s |
| 2 | `diff target/types/gherkin_pay.ts app/web/src/types/gherkin_pay.ts` | 0 | ✅ pass | <1s |
| 3 | `grep -c metadataUri app/web/src/types/gherkin_pay.ts` (returns 4) | 0 | ✅ pass | <1s |
| 4 | `cd app/web && bun run build` | 0 | ✅ pass | 22s |
| 5 | `solana program show ... \| grep -q "Data Length"` (diagnostic) | 0 | ✅ pass | 2s |

## Diagnostics

- **Deploy state:** `solana program show 2wL3PPjoG4UmVrNYZyXvxfTfV738AVCG8LHJPUEtxEeV --url devnet` — shows deploy slot, data length (422944 bytes), and upgrade authority
- **Type sync:** `grep -c metadataUri app/web/src/types/gherkin_pay.ts` should return 4; `diff target/types/gherkin_pay.ts app/web/src/types/gherkin_pay.ts` should be empty
- **Build health:** `cd app/web && bun run build` — compiles all routes; failure would indicate type mismatch
- **Failure shapes:** Deploy failures show in anchor CLI stderr (insufficient SOL, authority mismatch). Type mismatches surface as TypeScript errors in Next.js build output.

## Deviations

- **Keypair mismatch in worktree:** The worktree had a different `gherkin_pay-keypair.json` generated during S01 build, pointing to address `FcvdqJ6CkXhRujXUptNk1DwDcQepRsL2iLCCXvmrg2GY`. First deploy went to wrong address. Copied correct keypair from main repo and redeployed to the expected address. Not a plan-invalidating issue — standard worktree artifact drift.

## Known Issues

- An accidental deploy was made to `FcvdqJ6CkXhRujXUptNk1DwDcQepRsL2iLCCXvmrg2GY` on devnet (a few hundred bytes of SOL rent, negligible). That program is not referenced anywhere and can be ignored.

## Files Created/Modified

- `app/web/src/types/gherkin_pay.ts` — replaced stale types with generated version (4 metadataUri fields, name "gherkinPay")
- `target/deploy/gherkin_pay-keypair.json` — replaced with correct keypair from main repo matching program address 2wL3PPjoG4UmVrNYZyXvxfTfV738AVCG8LHJPUEtxEeV
- `.gsd/milestones/M005/slices/S03/tasks/T01-PLAN.md` — added Observability Impact section
- `.gsd/milestones/M005/slices/S03/S03-PLAN.md` — added diagnostic verification step
